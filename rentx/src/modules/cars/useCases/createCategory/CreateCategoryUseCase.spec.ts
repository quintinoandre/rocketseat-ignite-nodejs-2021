describe('Create category', () => {
	it('I hope 2 + 2 will be 4', () => {
		const sum = 2 + 2;

		const result = 4;

		expect(sum).toBe(result);
	});

	it('I hope 2 + 2 is not 5', () => {
		const sum = 2 + 2;

		const result = 5;

		expect(sum).not.toBe(result);
	});
});
